apiVersion: v1
kind: Service
metadata:
  name: {{.AppName}}-svc
  labels:
    app: {{.AppName}}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{.Port}}
      protocol: TCP
  selector:
    app: {{.AppName}}
